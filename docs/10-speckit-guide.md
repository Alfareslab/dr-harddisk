# 📖 دليل عمل Spec-Kit
> **إصدار الدليل:** 1.0.0
> **التاريخ:** 2026-02-21
> **الهدف:** توحيد سير العمل (Workflow) والانتقال من التخطيط اليدوي إلى التطوير الموجه بالمواصفات (Spec-Driven Development).

---

## 🎯 الفكرة العامة (The Concept)
بدلاً من بناء الميزة (Feature) أو المرحلة (Phase) دفعة واحدة، يقوم نظام `spec-kit` بتقسيم العمل إلى محطات (Gates). 
كل محطة تولّد ملفاً محدداً. **لا ننتقل للمحطة التالية إلا بعد مراجعة وموافقة المطور على مخرجات المحطة الحالية.**

هذا الأسلوب يضمن:
1. **انعدام الهلوسة:** الوكيل لا يقفز لكتابة كود بدون خطة مفصلة.
2. **التحكم الكامل:** المطور يرى المتطلبات، ثم الخطة التقنية، ثم خطوات التنفيذ، قبل كتابة سطر كود واحد.
3. **التوثيق الآلي:** كل ميزة تُوثق نفسها بنفسها داخل مجلد `specs/[feature-name]/`.

---

## 🛤️ مسار العمل الطبيعي (The Golden Path)

دورة حياة أي ميزة جديدة تمر بـ 4 أوامر أساسية بالترتيب التالي المستقل:

### المحطة 1: التوصيف (Specification)
> **الهدف:** الإجابة عن سؤال: *"ماذا نريد أن نبني؟ ولماذا؟"*

**الأمر المطلوب:**
```text
@/speckit.specify [وصف الميزة باللغة الطبيعية]
```
**مثال عملي:**
> `@/speckit.specify إنشاء صفحة عرض المقالات (Article Listing) تحتوي على تصفية وتصنيف للمقالات`

**المخرجات:** 
يقوم الوكيل بإنشاء مجلد `specs/[001-feature-name]/` وبداخله ملف `spec.md`. سيحتوي الملف على قصص المستخدمين (User Stories)، الحالات الخاصة (Edge Cases)، ومعايير النجاح (Success Criteria).

✅ **دورك:** مراجعة ملف `spec.md`. إذا كان مطابقاً للفكرة، تطلب من الوكيل الانتقال للمحطة 2.

---

### المحطة 2: التخطيط التقني (Planning)
> **الهدف:** الإجابة عن سؤال: *"كيف سنبني هذا برمجياً؟"*

**الأمر المطلوب:**
```text
@/speckit.plan
```
*(لا تحتاج لكتابة وصف هنا، الوكيل سيقرأ ملف `spec.md` من المحطة السابقة وتلقائياً يبدأ التخطيط).*

**المخرجات:** 
يقوم الوكيل بتوليد ملف `plan.md` داخل نفس المجلد `specs/[feature-name]/`. 
هذا الملف يحدد: المعمارية، الملفات التي سيتم إنشاؤها/تعديلها، وهل التخطيط يطابق `constitution.md` (دستور المشروع) أم لا.

✅ **دورك:** مراجعة ملف `plan.md`. إذا كانت التغييرات البرمجية المقترحة صحيحة، تعطي الأمر بالانتقال للمحطة 3.

---

### المحطة 3: تقسيم المهام (Task Breakdown)
> **الهدف:** تحويل الخطة التقنية إلى قائمة خطوات تنفيذية صغيرة جداً (Checklist).

**الأمر المطلوب:**
```text
@/speckit.tasks
```

**المخرجات:**
يقوم الوكيل بإنشاء ملف `tasks.md`. هذا الملف يحتوي على قائمة مهام (Todo) مرتبة حسب الاعتمادات (Dependencies). مثلاً: إنشاء واجهة الاستخدام قبل ربط البيانات.

✅ **دورك:** مراجعة القائمة لتتأكد أن الوكيل فهم التسلسل المنطقي للتنفيذ. يمكنك تعديل ترتيب المهام يدوياً هنا إذا أردت. بعد ذلك الانتقال للمحطة 4.

---

### المحطة 4: التنفيذ وكتابة الكود (Implementation)
> **الهدف:** كتابة الكود الفعلي بناءً على المهام.

**الأمر المطلوب:**
```text
@/speckit.implement
```

**المخرجات:**
يبدأ الوكيل بقراءة `tasks.md` وتنفيذ مهمة تلو الأخرى. سيقوم بتحديث الملف بوضع علامة `[x]` أمام كل مهمة ينجزها لتعرف كمدير مشروع أين وصل ومتى سينتهي.

✅ **دورك:** مراجعة الكود الناتج، وتجربة الميزة (Test)، ورفعها للـ Git بعد التأكد من نجاحها.

---

## 🛡️ أوامر مساعدة (Optional Tools)

هذه الأوامر تُستخدم عند الحاجة فقط للضبط والتدقيق في المحطات:

### 1 الاستيضاح (Clarification)
> يُستخدم **قبل المحطة 2 (التخطيط)** إذا كان الوصف المبدئي للميزة ناقصاً أو غامضاً وتريد من الذكاء الاصطناعي أن يطرح عليك أسئلة ذكية لتغطيته.
```text
@/speckit.clarify
```

### 2 التحليل والتدقيق (Analysis)
> يُستخدم **بعد المحطة 3 (المهام)** لمطابقة التوافق بين المواصفات، والخطة، والمهام والتأكد من عدم سقوط أي متطلب.
```text
@/speckit.analyze
```

### 3 التوثيق الآلي (Checklist)
> يُنشئ قائمة مراجعات جودة مخصصة (Custom QA Checklist) للميزة الحالية للمساعدة في الاختبار اليدوي بعد التنفيذ. 
```text
@/speckit.checklist
```

---

## 📌 ملخص دورة الحياة
1. 📝 فكرة ← `@/speckit.specify` ← إنشاء `spec.md`
2. 🏗️ تصميم ← `@/speckit.plan` ← إنشاء `plan.md`
3. 📋 تقسيم ← `@/speckit.tasks` ← إنشاء `tasks.md`
4. 👨‍💻 برمجة ← `@/speckit.implement` ← كود حقيقي + `[x]` في المهام

> **ملاحظة:** أي نقاش جانبي حول كود محدد أو إصلاح خطأ `Bug` يمكن أن يتم خارجه أو باستخدام خطط `Fix` أو `Audit` القديمة حسب تقدير المطور. هذا النظام مصمم حصرياً **لبناء الميزات (Features) والمراحل (Phases) الجديدة بشكل آمن.**
