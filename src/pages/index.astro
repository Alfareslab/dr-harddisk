---
// src/pages/index.astro
// ══════════════════════════════════════════════════════════════
// Homepage (Arabic) — Phase 1.3A
// Source: Plan 03-page-templates.md | Blueprint §3
// ══════════════════════════════════════════════════════════════

import BaseLayout from "../layouts/BaseLayout.astro";
import { brand } from "../config/location";
import { getPublishedPosts } from "../utils/content";
import ArticleCard from "../components/ArticleCard.astro";

const allPosts = await getPublishedPosts();
const latestPosts = allPosts.filter(post => post.data.lang === 'ar').slice(0, 3);

const cards = [
  { emoji: "💿", arName: "هارد ديسك", enName: "HDD", href: "/posts/?category=hdd" },
  { emoji: "💾", arName: "SSD", enName: "SSD", href: "/posts/?category=ssd" },
  { emoji: "⚡", arName: "NVMe", enName: "NVMe", href: "/posts/?category=ssd" },
  { emoji: "🃏", arName: "كرت ذاكرة", enName: "SD Card", href: "/posts/?category=flash" },
  { emoji: "🔌", arName: "فلاش ميموري", enName: "USB", href: "/posts/?category=flash" },
  { emoji: "📹", arName: "كاميرات مراقبة", enName: "DVR", href: "/posts/?category=dvr" },
  { emoji: "🖥️", arName: "سيرفر RAID", enName: "RAID", href: "/posts/?category=raid" },
  { emoji: "💬", arName: "محتاج مساعدة؟", enName: "Contact", href: "/contact/", isContact: true },
];
---

<BaseLayout title={brand.title} description={brand.tagline.ar}>
  
  {/* ─── Hero Section ─── */}
  <section data-section="hero" class="relative overflow-hidden bg-gradient-to-b from-surface to-[#eff1f3] py-16 md:py-24 px-4">
    <div class="max-w-6xl mx-auto text-center">
      
      <h1 class="text-[#0b4f6c] font-bold text-4xl md:text-6xl mb-6 leading-tight">
        إيه مشكلة جهازك؟
      </h1>
      
      <p class="text-gray-600 text-lg md:text-xl mb-12 max-w-2xl mx-auto">
        {brand.name} — موسوعتك التقنية لفهم مشاكل التخزين واستعادة البيانات
      </p>

      {/* Cards Grid */}
      <div class="grid grid-cols-2 md:grid-cols-4 gap-4 md:gap-6">
        {cards.map((card) => (
          <a
            href={card.href}
            class:list={[
              "group block p-6 rounded-xl border transition-all duration-300 transform hover:scale-[1.02] hover:shadow-md",
              card.isContact 
                ? "bg-[#f0fdf4] border-[#bbf7d0] hover:border-primary-500" 
                : "bg-white border-border hover:border-[#0b4f6c]/30"
            ]}
          >
            <span class="block text-4xl mb-3 filter grayscale-[0.2] group-hover:grayscale-0 transition-all">
              {card.emoji}
            </span>
            <span class="block font-bold text-gray-800 text-lg group-hover:text-[#0b4f6c] transition-colors">
              {card.arName}
            </span>
            <span class="block text-xs text-gray-400 mt-1 font-sans group-hover:text-gray-600 transition-colors">
              {card.enName}
            </span>
          </a>
        ))}
      </div>

    </div>
  </section>

  {/* ─── Latest Articles ─── */}
  <section class="py-16 md:py-24 bg-white px-4 border-t border-border">
    <div class="max-w-6xl mx-auto">
      <div class="text-center mb-12">
        <h2 class="text-[#0b4f6c] font-bold text-3xl mb-4">أحدث الشروحات التقنية</h2>
        <p class="text-gray-500">مقالات من المختبر — بعيداً عن الخرافات والمعلومات المغلوطة</p>
      </div>
      
      {latestPosts.length > 0 ? (
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
          {latestPosts.map(post => (
            <ArticleCard post={post} locale="ar" />
          ))}
        </div>
      ) : (
        <div class="p-8 border-2 border-dashed border-gray-200 rounded-xl bg-gray-50 text-center">
          <p class="text-gray-400">لم يتم نشر مقالات بعد — تابعنا قريباً.</p>
        </div>
      )}

      <div class="text-center mt-12">
        <a 
          href="/posts/" 
          class="inline-flex items-center justify-center px-6 py-3 bg-white border-2 border-primary-600 text-primary-600 font-bold rounded-lg hover:bg-primary-50 transition-colors"
        >
          عرض جميع المقالات ←
        </a>
      </div>
    </div>
  </section>

  {/* ─── Success Stories (Placeholder) ─── */}
  <section class="py-16 md:py-24 bg-[#f1f5f9] px-4">
    <div class="max-w-4xl mx-auto text-center">
      <h2 class="text-[#0b4f6c] font-bold text-3xl mb-4">قصص حقيقية من المختبر</h2>
      <p class="text-gray-500 mb-8">كل حالة تحدي وكل نجاح بنتعلم منه</p>
      
      <div class="p-8 border-2 border-dashed border-gray-300 rounded-xl bg-white/50">
        <p class="text-gray-400">Content coming soon.</p>
      </div>
    </div>
  </section>

  {/* ─── About Section (Static Brand) ─── */}
  <section class="py-20 bg-gradient-to-br from-[#0b4f6c] to-[#07364a] text-white px-4 text-center">
    <div class="max-w-3xl mx-auto">
      <h2 class="font-bold text-3xl md:text-4xl mb-6">Dr. Hard Disk</h2>
      
      <div class="space-y-4 text-gray-200 text-lg leading-relaxed mb-10">
        <p>
          أكثر من 8 سنوات من الخبرة في التعامل مع أعقد حالات فقدان البيانات.
          نحن لا نستخدم برامج مجانية، بل نتعامل مع وسائط التخزين في بيئة معملية متخصصة.
        </p>
        <p>
          هدفنا هو نشر الوعي التقني الصحيح وحمايتك من الاستغلال أو التجارب الخاطئة التي قد تدمر بياناتك للأبد.
        </p>
      </div>

      <div class="flex flex-wrap justify-center gap-3 md:gap-4 mb-10">
        <span class="inline-flex items-center px-4 py-2 rounded-full bg-white/10 text-sm backdrop-blur-sm border border-white/10">
          PC-3000 Certified
        </span>
        <span class="inline-flex items-center px-4 py-2 rounded-full bg-white/10 text-sm backdrop-blur-sm border border-white/10">
          MRT Ultra Lab
        </span>
        <span class="inline-flex items-center px-4 py-2 rounded-full bg-white/10 text-sm backdrop-blur-sm border border-white/10">
          Clean Room Class 100
        </span>
      </div>

      <a 
        href="/about/" 
        class="inline-block text-white font-bold border-b-2 border-primary-500 pb-1 hover:text-primary-400 hover:border-primary-400 transition-colors"
      >
        تعرف على Dr. Hard Disk أكتر ←
      </a>
    </div>
  </section>

</BaseLayout>
