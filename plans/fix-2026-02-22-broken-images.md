# تقرير إصلاح: روابط الصور المعطوبة وتداخل علامة التصنيف

> **التاريخ:** 2026-02-22  
> **المرجع:** ملاحظة العميل على תصوير الشاشة  

### المشكلة:
علامة التصنيف (Category Badge) تتداخل مع نص يظهر داخل مساحة الصورة في بطاقات المقالات (Article Cards)، كما يظهر تماماً في لقطة الشاشة المرفقة من قبلكم.

### السبب:
الصور الخاصة بالمقالات (Hero Images) **لا تعمل من الأساس (Broken Images)**. 
عند فشل تحميل الصورة، يقوم المتصفح بعرض "النص البديل" (Alt text) الخاص بها - والذي برمجناه ليكون مساوياً لعنوان المقال - داخل المربع المخصص للصورة (`h-48`). 
وبما أن علامة التصنيف (الـ Badge) مبرمجة لتظهر بموضع مطلق (Absolute) في أعلى يسار مساحة الصورة، فقد تداخلت مع النص البديل الخاص بالصورة المعطوبة.

سبب تعطل الصورة هو مسارات جلب الصور ديناميكياً في ملف `ArticleCard.astro`: 
الصور مخزنة في ملفات المحتوى بمسار `../../assets/images/...` (نسبةً لملفات المحتوى).
ولكن مكون الـ Card يحاول استيرادها بواسطة كود `import.meta.glob` بمسار نسبي خاطئ (نسبةً لمجلد الـ components)، مما يجعله يفشل في العثور على مسار الصورة. (تم تطبيق هذا الحل الصحيح مسبقاً في `PostLayout.astro` ولكن تم نسيانه في `ArticleCard`).

### الحل المقترح:
1. توحيد طريقة جلب الصور في `ArticleCard.astro` لتعتمد على المسارات المطلقة من جذر المشروع كما في صفحة المقال الفردي: `import.meta.glob('/src/assets/images/**/*')`
2. معالجة المتغير `heroImage` الوارد من المقال بحيث يتم استبدال `../../` بـ `/src/` ليتطابق تماماً مع مفاتيح كائن `images`.
3. التأكد من أن الصور ستعمل محلياً، وبمجرد ظهور الصور ستختفي النصوص البديلة وسيبدو شكل "علامة التصنيف" سليماً وغير متداخل.

### الملفات المتأثرة:
- `src/components/ArticleCard.astro`

### التنفيذ:
المشكلة تم تحليلها بالكامل وجاهزة للتنفيذ بعد موافقتكم، التعديل بسيط وسيحل مشكلة اختفاء الصور في جميع الكروت. (✅ تم الإنجاز 2026-02-22 23:12)

---
✅ **منفذ بواسطة:** Antigravity (Gemini)
📅 **التاريخ:** 2026-02-22 23:12
