# 🗺️ Rebranding Execution Plan — Datacodex
> **الإصدار:** 1.0.0
> **التاريخ:** 2026-02-23
> **المنهجية:** Multi-Model Development

## 🎯 الهدف العام
تعديل هوية المشروع بالكامل من "Datacodex" (بنسختيه العربية والإنجليزية) إلى الاسم الجديد المعتمد **"Datacodex"** (باللغة الإنجليزية فقط)، مع مراعاة الأبعاد الجديدة للشعار (21:9) في واجهة المستخدم.

## 📊 إحصائية رقمية (Digital Statistics)
بناءً على الفحص الشامل لملفات المشروع، تم رصد الظهور التالي للاسم القديم:
- **"Datacodex" (بمختلف أشكالها):** ~345 مرة.
- **"Datacodex" (أو Datacodex):** ~40 مرة.
- **الإجمالي التقريبي للمعالجة:** ~385 موضعاً موزعة على ملفات التكوين، المحتوى (MDX)، مكونات الواجهة (Astro)، ووثائق المشروع (MD).

## 📍 خريطة المواضع (Locations Map)
سيتم تطبيق عملية الاستبدال (Search and Replace) في الأقسام والملفات التالية:

- **ملفات التكوين (Configuration):**
  - `astro.config.mjs` (تحديث التعليقات إذا لزم الأمر).
  - `src/config/location.ts` (تعديل استدعاءات `brand.name` و `brand.nameEn` إلى `Datacodex`).
- **مكونات الواجهة (UI Components):**
  - `src/components/layout/NavBar.astro` (تحديث اللوجو والنص).
  - `src/components/layout/Footer.astro` (تحديث اللوجو واسم البراند في حقوق النشر).
  - `src/components/ui/HeroCard.astro` وأي مكونات أخرى تعرض اسم البراند.
- **محتوى الصفحات والمقالات (Pages & Posts):**
  - ملفات `src/pages/*.astro` مثل `index.astro`, `contact.astro`, `about.astro`.
  - ملفات المقالات في `src/content/posts/*.mdx` (استبدال الاسم في النصوص والـ Frontmatter).
- **وثائق التخطيط والدستور (Documentation & Plans):**
  - `master-constitution.md`, `project-key.md`, `project-context.md`.
  - مجلد `plans/` ومجلد `docs/` ومجلد `Research/` (لتحديث الرؤية وكل الإشارات القديمة).

## 🛠️ تعديلات الكود المتوقعة (Code Modifications)
للتوافق مع أبعاد الشعار الجديد **21:9**، سيتم تعديل حاوية الشعار في كل من `NavBar.astro` و `Footer.astro`.

**في `src/components/layout/NavBar.astro`:**
سيتم إزالة الأيقونة النصية (`💿`) واسم البراند المكتوب كـ Text، واستبدالها بوسم `<img />` أو `<Icon />` كالتالي:
```html
<a href={isAr ? '/' : '/en/'} class="flex items-center z-50 group hover:opacity-90 transition-opacity">
  <img 
    src="/images/logo.png" /* سيتم تحديد مسار الشعار لاحقاً */
    alt="Datacodex Logo" 
    class="w-48 sm:w-56 aspect-[21/9] object-contain" 
  />
</a>
```

**في `src/components/layout/Footer.astro`:**
بنفس الطريقة سيتم تأمين مساحة للشعار في قسم Brand Zone:
```html
<div class="group flex flex-col items-center gap-4">
  <img 
    src="/images/logo.png" /* شعار Footer عادة قد يكون بلون مختلف أو نفس الشعار */
    alt="Datacodex Logo" 
    class="w-56 aspect-[21/9] object-contain grayscale group-hover:grayscale-0 transition-all duration-300" 
  />
  <!-- إزالة النصوص الفرعية للاسم إن لم تكن مطلوبة -->
</div>
```

---

## 📅 المراحل التنفيذية

### **المرحلة 1: تحديث الدستور والوثائق (Documentation) 📄**
> **النموذج:** `Antigravity / Gemini Pro` 🟠
> **الهدف:** استبدال الاسم في `master-constitution.md`، ملفات التخطيط، وملفات البحث.
> **يعتمد على:** موافقة المستخدم ✅

| تنفيذ | مراجعة | المهمة |
| :---: | :---: | :--- |
| `[ ]` | `[ ]` | استبدال الاسم في الدستور وملفات السياق العام. |
| `[ ]` | `[ ]` | استبدال الاسم في ملفات خطط العمل والمحتوى الموجودة في `Research/`. |

### **المرحلة 2: تحديث الكود الأساسي والواجهة (Core & UI) 💻**
> **النموذج:** `Antigravity / Gemini Pro` 🟠
> **الهدف:** تطبيق تعديلات الشعار واستبدال الاسم بمتغيرات المشروع `src/config/location.ts` وغيرها.
> **يعتمد على:** المرحلة 1 ✅

| تنفيذ | مراجعة | المهمة |
| :---: | :---: | :--- |
| `[ ]` | `[ ]` | تعديل `location.ts` ليحتوي على `Datacodex` كاسم وحيد. |
| `[ ]` | `[ ]` | تعديل `NavBar.astro` و `Footer.astro` لدعم الشعار بنسبة `21:9`. |
| `[ ]` | `[ ]` | تحديث الـ Meta Data والأسماء في ملفات `src/pages/*.astro`. |

### **المرحلة 3: تحديث المقالات الحالية (Content) 📝**
> **النموذج:** `Antigravity / Gemini Flash` 🟢
> **الهدف:** تنظيف محتوى المقالات الـ MDX من الاسم القديم تماماً.
> **يعتمد على:** المرحلة 2 ✅

| تنفيذ | مراجعة | المهمة |
| :---: | :---: | :--- |
| `[ ]` | `[ ]` | فحص واستبدال الاسم في `src/content/posts/*.mdx`. |

---

## 🛑 بوابات ما قبل التنفيذ (محققة):
- [x] تم الفحص الشامل للمتطلبات.
- [x] جميع مواقع التعديل واضحة وتم توثيقها مسبقاً.
- [x] تغيير الكود لتعديلات الشعار (`aspect-[21/9]`) يتبع أفضل ممارسات Tailwind.

**🔄 برومبت بدء المرحلة 1 (عند الموافقة):**
يرجى تأكيد هذه الخطة لبدء عملية الاستبدال الشامل.
