# خطة التنفيذ: تثبيت أدوات Spec-Kit
> **المرحلة:** Phase 1.6
> **التاريخ:** 2026-02-21
> **الحالة:** 🔄 قيد العمل

## 1. الأهداف
تثبيت وتحضير أداة `spec-kit` الرسمية من GitHub في المشروع لدعم منهجية Spec-Driven Development باستخدام وكيل Antigravity.

## 2. الخطوات التنفيذية
1. **تثبيت `uv`**: تثبيت أداة إدارة الحزم `uv` عبر `pip` إذا لم تكن متوفرة.
2. **تثبيت `specify-cli`**: تشغيل أمر التثبيت الشامل من مستودع `spec-kit` عبر `uv`.
3. **تهيئة المشروع**: تشغيل الأمر `specify init . --ai agy --force` للتهيئة داخل المشروع الحالي وربطه بوكيل Antigravity.

## 3. الملفات المتأثرة
- **إضافة**: مجلد `.specify` وملفاته الداخلية (Templates + Memory).
- **تحديث**: `project-context.md`، `project-key.md`، `changelog.md` (بعد التنفيذ).

## 4. المراجعة
بانتظار موافقة المطور للبدء في تشغيل الأوامر بالـ Terminal.
