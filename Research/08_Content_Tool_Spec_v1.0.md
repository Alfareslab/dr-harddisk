

# 08 — Content Tool Specification

## ملف مواصفات أداة توليد المحتوى

**إصدار:** v1.1
**تاريخ:** فبراير 2026
**الحالة:** مسودة للاعتماد
**يعتمد على:** TAD v4.2 (ملف 05)، SEO v4 (ملف 06)، Content Plan v1.3 (ملف 07)، Homepage Blueprint v1.0 (ملف 09)
**يُحدّث:** TAD v4.2 (إضافة حقول frontmatter)، Content Plan v1.3 (استبدال Keystatic)

---

## Changelog

- v1.1 (February 2026): Updated to match Homepage Blueprint (File 09) and
  updated project files (TAD v4.2, UX v3.2, Content Plan v1.3).
  - Replaced CallToAction with GentleNote in articles, services, and cases
  - Separated PanicButton (in-article emergency banner) from FloatingWhatsApp (layout-level)
  - Added Cases (Success Stories) support in MVP (moved from Phase 5 to Phase 1)
  - Added ReelEmbed component for social media videos
  - Added content policy validation (banned selling phrases) — Section 10.4
  - Added location mention warnings — Section 10.5
  - Added image policy (AI-generated only, stored prompts) — Section 7.5
  - Added brand voice rules — Section 1.5
  - Added location.ts config reference — Section 11.3

- v1.0 (January 2025): Initial specification.
  Replaced Keystatic workflow with Content Generator Tool pipeline.
  Added taxonomy system (category, brands, symptoms, difficulty).
  Defined NotebookLM output format and component injection rules.

---

## 1. ملخص تنفيذي (Executive Summary)

### 1.1 ما هي هذه الأداة؟

هي صفحة ويب واحدة (Single HTML File) تعمل في المتصفح بدون سيرفر وبدون إنترنت (في المرحلة الأولى). وظيفتها تحويل النص المُولَّد من NotebookLM إلى ملفات MDX جاهزة للنشر على موقع Datacodex.

### 1.2 لماذا نبنيها؟

صاحب الموقع ليس مبرمجاً. كتابة ملفات MDX يدوياً تتطلب التعامل مع frontmatter وصيغ YAML ومسارات صور ومكونات MDX. أي خطأ صغير في المسافات أو الأقواس يمنع الموقع من البناء. الأداة تلغي هذا الخطر تماماً.

### 1.3 لماذا ليس Keystatic؟

تم إلغاء Keystatic للأسباب المسجلة في ملف 02 (Risk Assessment v3): تعقيد المصادقة مع GitHub، صعوبة الاستخدام على الموبايل، عدم دعم العربية بشكل كافٍ، وإضافة طبقة تعقيد لا يحتاجها مشغّل فردي.

### 1.4 ما الذي يتغير في الخطط الحالية؟

هذا الملف يُدخل التغييرات التالية على الخطط المعتمدة:

**تغييرات على TAD v4.2 (ملف 05):**
- إضافة 4 حقول جديدة في الـ frontmatter: category, brands, symptoms, difficulty
- حذف حقل panicLevel واستبداله بمنطق مشتق من difficulty
- إضافة صفحة /admin (أو رابط سري) لاستضافة الأداة

**تغييرات على Content Plan v1.3 (ملف 07):**
- استبدال Keystatic بالأداة المحلية كآلية إدخال المحتوى
- إضافة تفاصيل سير العمل الجديد (NotebookLM → الأداة → GitHub → Cloudflare)

**لا تغييرات على:**
- ملف 01 (الرؤية) — لا يتأثر
- ملف 03 (المتطلبات) — لا يتأثر
- ملف 04 (UX) — المكونات كما هي، فقط طريقة حقنها تتم عبر الأداة
- ملف 06 (SEO) — لا يتأثر، الأداة تلتزم بمعايير SEO المسجلة

**تغييرات v1.1 الإضافية:**
- استبدال CallToAction بـ GentleNote (نبرة معرفية بدل بيعية — ملف 09)
- فصل PanicButton (شريط طوارئ داخل المقال) عن FloatingWhatsApp (زر عائم في الـ layout)
- إضافة دعم Cases (قصص النجاح) في المرحلة الأولى بدلاً من المرحلة 5
- إضافة ReelEmbed لتضمين فيديوهات سوشيال ميديا
- إضافة تحقق من الممنوعات اللغوية وذكر الأماكن
- إضافة سياسة الصور (AI-generated فقط)

### 1.5 Brand Voice Rules — NEW v1.1

The tool should enforce these rules in generated frontmatter and content:

1. **Brand name:** Always "Datacodex" — never a personal name
2. **Voice:** Passive/third person — "Datacodex has 8+ years experience"
   NOT "I am Ahmad and I have experience"
3. **Tone:** Expert teacher, not salesperson. The website is an **encyclopedia**, not a shop.
4. **Author field:** Always "Datacodex" in article meta
5. **No personal photos:** All images are AI-generated or equipment photos
6. **No selling language:** The tool warns if content contains banned sales phrases (see Section 10.4)

---

## 2. التصنيفات — النظام الجديد (Taxonomy System)

### 2.1 المبدأ

هذا نظام تصنيف جديد لم يكن موجوداً في الخطط السابقة. الخطة الأصلية كانت تقول "مفيش نظام tags أو categories في الـ MVP". هذا القرار يتم تعديله الآن لأن موقع تسويق بالمحتوى في مجال تقني يحتاج تصنيفاً من اليوم الأول.

الملفات تبقى في هيكل مسطح (Flat Structure) كما هو متفق عليه في TAD v4.2. التصنيف يتم عبر حقول في الـ frontmatter وليس عبر مجلدات فرعية.

### 2.2 الحقول الجديدة

**category — القسم الرئيسي**

```yaml
# Required — Single value — Enum
category: "hdd"
```

القيم المسموحة والنهائية:

| Value | Arabic Label | English Label | Description |
|-------|-------------|---------------|-------------|
| hdd | هارد ديسك | Hard Disk Drive | أقراص صلبة ميكانيكية |
| ssd | SSD | Solid State Drive | أقراص صلبة إلكترونية |
| raid | سيرفر / RAID | RAID / Server | أنظمة RAID وسيرفرات |
| flash | فلاش / كرت ذاكرة | Flash / Memory Card | USB و SD Cards |
| mobile | موبايل / تابلت | Mobile / Tablet | هواتف وأجهزة لوحية |
| nas | NAS | NAS | أجهزة التخزين الشبكي |
| general | عام | General | مقالات عامة عن استعادة البيانات |

**brands — العلامات التجارية**

```yaml
# Optional — Array (0-3) — Enum
brands:
  - "western-digital"
  - "seagate"
```

القيم المسموحة:

| Value | Display Name |
|-------|-------------|
| western-digital | Western Digital / ويسترن ديجيتال |
| seagate | Seagate / سيجيت |
| samsung | Samsung / سامسونج |
| toshiba | Toshiba / توشيبا |
| kingston | Kingston / كينجستون |
| crucial | Crucial / كروشيال |
| sandisk | SanDisk / سان ديسك |
| hikvision | Hikvision / هيكفيجن |
| other | Other / أخرى |

**symptoms — الأعراض والمشاكل**

```yaml
# Optional — Array (0-5) — Enum
symptoms:
  - "clicking-sound"
  - "not-detected"
```

القيم المسموحة:

| Value | Arabic | English | وصف مختصر |
|-------|--------|---------|-----------|
| clicking-sound | صوت طقطقة | Clicking Sound | صوت نقر متكرر من الهارد |
| not-detected | غير معروف للجهاز | Not Detected | الكمبيوتر لا يرى الوحدة |
| water-damage | تلف بالماء | Water Damage | سقوط ماء أو غمر |
| fire-damage | تلف بالحريق | Fire Damage | حريق أو حرارة شديدة |
| deleted-files | ملفات محذوفة | Deleted Files | حذف عرضي |
| formatted-drive | فورمات بالخطأ | Formatted Drive | تهيئة غير مقصودة |
| ransomware | فيروس فدية | Ransomware | تشفير الملفات بفيروس |
| bad-sectors | باد سيكتور | Bad Sectors | قطاعات تالفة |
| firmware-corruption | تلف فيرموير | Firmware Issue | مشكلة في البرنامج الثابت |
| pcb-failure | عطل في البوردة | PCB Failure | تلف اللوحة الإلكترونية |
| head-crash | تلف الرؤوس | Head Crash | ارتطام رؤوس القراءة |
| motor-failure | عطل المحرك | Motor Failure | توقف محرك الدوران |
| electrical-surge | صدمة كهربائية | Electrical Surge | تلف بسبب الكهرباء |
| physical-damage | تلف فيزيائي | Physical Damage | سقوط أو كسر |
| logical-damage | تلف منطقي | Logical Damage | مشكلة برمجية بدون تلف مادي |

**difficulty — مستوى الصعوبة**

```yaml
# Required — Single value — Enum
# Replaces: panicLevel from TAD v4
difficulty: "critical"
```

| Value | Arabic | English | سلوك الموقع |
|-------|--------|---------|------------|
| simple | بسيط — برمجي | Simple | لا مكونات طوارئ |
| moderate | متوسط — أدوات متخصصة | Moderate | لا مكونات طوارئ |
| critical | حرج — غرفة نظيفة | Critical | يظهر PanicButton + WarningBox تلقائياً |

ملاحظة مهمة: هذا الحقل يحل محل panicLevel الموجود في TAD v4. القاعدة: `difficulty: critical` يساوي `panicLevel: high` القديم.

### 2.3 كيف تظهر التصنيفات على الموقع

**المرحلة الأولى (MVP):**
المقالات تظهر على الصفحة الرئيسية مع إمكانية الفلترة حسب category. لا توجد صفحات أقسام مستقلة بعد.

**المرحلة التالية:**
Astro يبني صفحات أقسام ديناميكياً وقت البناء. الزائر يرى:
- صفحة لكل category → مثلاً /ar/category/hdd
- صفحة لكل brand → مثلاً /ar/brand/western-digital
- صفحة لكل symptom → مثلاً /ar/symptom/clicking-sound

البناء يتم من نفس الملفات المسطحة بدون تكرار. Astro يقرأ الـ frontmatter ويجمع المقالات المناسبة لكل صفحة.

---

## 3. الـ Frontmatter النهائي المُحدَّث

### 3.1 للمقالات (Posts)

هذا يحل محل الـ frontmatter المذكور في TAD v4.2 ويضيف عليه:

```yaml
---
# ─── الهوية (من TAD v4 — بدون تغيير) ───
title: "استعادة بيانات هارد ويسترن ديجيتال يصدر صوت طقطقة"
description: "دليل شامل لاستعادة البيانات من هارد WD يصدر صوت طقطقة"
pubDate: "2025-01-15"
heroImage: "../../assets/images/posts/wd-clicking-drive-recovery.webp"
lang: "ar"
translationID: "wd-clicking-drive-recovery"

# ─── التصنيف (جديد — يُضاف على TAD v4) ───
category: "hdd"
brands:
  - "western-digital"
symptoms:
  - "clicking-sound"
  - "head-crash"
difficulty: "critical"

# ─── تحكم (جديد — اختياري) ───
draft: false
featured: false
---
```

### 3.2 للخدمات (Services)

```yaml
---
title: "خدمة استعادة بيانات الهارد ديسك"
description: "نستعيد بياناتك من جميع أنواع الهارد ديسك"
pubDate: "2025-01-15"
heroImage: "../../assets/images/services/hdd-recovery-service.webp"
lang: "ar"
translationID: "hdd-recovery-service"
category: "hdd"
brands: []
symptoms: []
difficulty: "moderate"
draft: false
---
```

### 3.3 لقصص النجاح (Cases) — NEW v1.1

```yaml
---
# ─── الهوية ───
title: "هارد WD Blue — سقوط من متر ونصف"
description: "قصة استعادة بيانات من هارد ويسترن ديجيتال سقط من ارتفاع متر ونصف"
pubDate: "2025-01-15"
heroImage: "../../assets/images/cases/case-slug.webp"
lang: "ar"
translationID: "case-slug"

# ─── التصنيف ───
category: "hdd"
brands:
  - "western-digital"
symptoms:
  - "clicking-sound"
difficulty: "critical"

# ─── حقول خاصة بالقصص (NEW v1.1) ───
device_type: "hdd"
recovery_percentage: 98
badge: "success"
problem: "الهارد يصدر صوت طقطقة بعد السقوط"
solution: "استبدال رؤوس القراءة في الغرفة النظيفة"
result: "تم استعادة 98% من البيانات — صور عائلية لـ 10 سنوات"

# ─── تحكم ───
draft: false
featured: false
---
```

### 3.4 جدول الحقول الكامل — UPDATED v1.1

| الحقل | النوع | إلزامي | المصدر | ملاحظات |
|-------|-------|--------|--------|---------|
| title | string | ✅ | TAD v4 | 10-120 حرف |
| description | string | ✅ | TAD v4 | 50-160 حرف (SEO) |
| pubDate | date | ✅ | TAD v4 | YYYY-MM-DD |
| heroImage | string | ✅ | TAD v4 | مسار نسبي من src/assets |
| lang | enum | ✅ | TAD v4 | "ar" أو "en" |
| translationID | string | ✅ | TAD v4 | يطابق الـ slug، متطابق في الملفين |
| category | enum | ✅ | **جديد** | قيمة واحدة من القائمة |
| brands | array | ❌ | **جديد** | 0-3 قيم من القائمة |
| symptoms | array | ❌ | **جديد** | 0-5 قيم من القائمة |
| difficulty | enum | ✅ | **جديد** — يحل محل panicLevel | simple/moderate/critical |
| draft | boolean | ❌ | **جديد** | الافتراضي false |
| featured | boolean | ❌ | **جديد** | الافتراضي false |
| device_type | enum | ✅ Cases only | **جديد v1.1** | hdd / ssd / raid / flash |
| recovery_percentage | number | ✅ Cases only | **جديد v1.1** | 0-100 |
| badge | enum | ✅ Cases only | **جديد v1.1** | success / challenge |
| problem | string | ✅ Cases only | **جديد v1.1** | وصف المشكلة بسطر واحد |
| solution | string | ✅ Cases only | **جديد v1.1** | وصف الحل بسطر واحد |
| result | string | ✅ Cases only | **جديد v1.1** | وصف النتيجة بسطر واحد |

---

## 4. صيغة الإدخال (NotebookLM Output Format)

### 4.1 المبدأ

NotebookLM يولّد المقال بصيغة نصية محددة تستخدم علامات (Markers) للفصل بين الأقسام. الأداة تعتمد على هذه العلامات لتحليل النص.

### 4.2 الصيغة الكاملة المعتمدة — UPDATED v1.1

```
<<<ARTICLE_START>>>
TITLE_AR: العنوان بالعربي
TITLE_EN: العنوان بالإنجليزي
DESC_AR: وصف عربي لا يزيد عن 155 حرف
DESC_EN: وصف إنجليزي لا يزيد عن 155 حرف
TYPE: POST أو SERVICE أو CASE
CATEGORY: hdd
BRANDS: western-digital, seagate
SYMPTOMS: clicking-sound, head-crash
DIFFICULTY: critical

# ─── الحقول التالية مطلوبة فقط عند TYPE: CASE ───
DEVICE_TYPE: hdd
RECOVERY_PERCENTAGE: 98
BADGE: success
PROBLEM: الهارد يصدر صوت طقطقة بعد السقوط ولا يعمل
SOLUTION: استبدال رؤوس القراءة في الغرفة النظيفة باستخدام PC-3000
RESULT: تم استعادة 98% من البيانات — صور عائلية لـ 10 سنوات

<<<AR_CONTENT>>>

المحتوى العربي هنا...
عناوين بـ ## و ###
فقرات عادية

:::warning
نص تحذيري هنا
:::

<<<IMAGE>>>
وصف: وصف بالعربي للصورة المطلوبة
نوع: صورة حقيقية أو رسم توضيحي أو إنفوجرافيك
AI_PROMPT: A detailed English prompt for AI image generation...
ALT_AR: نص بديل بالعربي
ALT_EN: Alternative text in English
<<<END_IMAGE>>>

<<<REEL_URL: https://tiktok.com/@drhdd/video/xxxxx>>>

بقية المحتوى...

<<<EN_CONTENT>>>

English content here...
Same structure, not literal translation

<<<IMAGE>>>
Same image block repeated for reference
<<<END_IMAGE>>>

<<<REEL_URL: https://tiktok.com/@drhdd/video/xxxxx>>>

Rest of content...

<<<ARTICLE_END>>>
```

### 4.3 ملاحظات على الصيغة — UPDATED v1.1

- العلامات تستخدم `<<<` و `>>>` (ثلاثة أقواس) وليس `===` كما نوقش سابقاً. السبب: الأقواس الثلاثية أقل احتمالاً أن تظهر في المحتوى العادي.

- حقل TYPE يحدد نوع المحتوى: POST للمقالات أو SERVICE للخدمات أو CASE لقصص النجاح. هذا يؤثر على المسار في الـ ZIP والمكونات المحقونة.

- **[v1.1]** عندما يكون TYPE هو CASE، يجب تضمين الحقول الإضافية: DEVICE_TYPE، RECOVERY_PERCENTAGE، BADGE، PROBLEM، SOLUTION، RESULT. الأداة تتحقق من وجودها وتعرض خطأ لو كانت مفقودة.

- حقول CATEGORY و BRANDS و SYMPTOMS و DIFFICULTY يملؤها NotebookLM تلقائياً بناءً على محتوى المقال. الأداة تعرضها للمراجعة والتعديل.

- الصور بعلامات `<<<IMAGE>>>` و `<<<END_IMAGE>>>`. NotebookLM يضعها في المكان المناسب داخل المقال. الأداة تعرض لكل صورة خيار الرفع أو التجاوز.

- **[v1.1]** الريلز بعلامات `<<<REEL_URL: url>>>`. الأداة تحوّل العلامة إلى مكون `<ReelEmbed>` مع تحديد المنصة تلقائياً من الرابط.

### 4.4 مرجع العلامات الكامل (Marker Reference) — UPDATED v1.1

| العلامة | الوظيفة | ملاحظات |
|---------|---------|---------|
| `<<<ARTICLE_START>>>` | بداية المقال | إلزامي |
| `<<<ARTICLE_END>>>` | نهاية المقال | إلزامي |
| `<<<AR_CONTENT>>>` | بداية المحتوى العربي | إلزامي |
| `<<<EN_CONTENT>>>` | بداية المحتوى الإنجليزي | إلزامي |
| `<<<IMAGE>>>` ... `<<<END_IMAGE>>>` | صورة مع بياناتها | اختياري — متعدد |
| `:::warning` ... `:::` | صندوق تحذير | اختياري — يُحوّل إلى WarningBox |
| `<<<VIDEO_ID: id>>>` | فيديو يوتيوب | اختياري |
| `<<<REEL_URL: url>>>` | ريل TikTok أو Instagram | اختياري — **NEW v1.1** |

### 4.5 مثال صيغة قصة نجاح (Case Example) — NEW v1.1

```
<<<ARTICLE_START>>>
TITLE_AR: هارد WD — سقوط من متر ونصف
TITLE_EN: WD Hard Drive — 1.5 Meter Drop Recovery
DESC_AR: قصة استعادة بيانات من هارد ويسترن ديجيتال سقط من ارتفاع متر ونصف
DESC_EN: Data recovery story from a Western Digital drive that fell 1.5 meters
TYPE: CASE
CATEGORY: hdd
BRANDS: western-digital
SYMPTOMS: clicking-sound, head-crash
DIFFICULTY: critical
DEVICE_TYPE: hdd
RECOVERY_PERCENTAGE: 98
BADGE: success
PROBLEM: الهارد يصدر صوت طقطقة بعد السقوط ولا يعمل
SOLUTION: استبدال رؤوس القراءة في الغرفة النظيفة باستخدام PC-3000
RESULT: تم استعادة 98% من البيانات — صور عائلية لـ 10 سنوات
<<<AR_CONTENT>>>

## المشكلة — ماذا حدث؟

جاءنا هارد WD Blue 1TB بعد ما وقع من على المكتب...

<<<IMAGE>>>
وصف: هارد WD Blue مفتوح تظهر خدوش على الأسطوانات
نوع: صورة توضيحية
AI_PROMPT: Open WD Blue hard drive showing damaged platters with visible scratches, clean room environment, professional data recovery, top-down view
ALT_AR: هارد WD Blue مفتوح يظهر تلف في الأسطوانات
ALT_EN: Open WD Blue hard drive showing platter damage
<<<END_IMAGE>>>

## التشخيص — ماذا وجدنا؟

الفحص بجهاز PC-3000 أظهر تلف كامل في رؤوس القراءة...

## التحدي — لماذا هذه الحالة صعبة؟

الأسطوانات كان فيها خدوش سطحية في المنطقة الأولى...

## الحل — كيف تمت استعادة البيانات؟

في الغرفة النظيفة (Class 100) تم استبدال رؤوس القراءة...

## النتيجة

تم استعادة 98% من البيانات خلال 5 أيام عمل...

## الدروس المستفادة

لو الهارد وقع، أهم شيء ما تشغله تاني...

<<<EN_CONTENT>>>

## The Problem — What Happened?

A WD Blue 1TB drive was brought in after falling from a desk...

## Diagnosis — What We Found

PC-3000 analysis revealed complete read/write head failure...

## The Challenge — Why This Case Was Difficult

The platters had surface scratches in the first zone...

## The Solution — How Data Was Recovered

In our Class 100 clean room, the read/write heads were replaced...

## The Result

98% of data was recovered within 5 business days...

## Lessons Learned

If a hard drive falls, the most important thing is to NOT power it on again...

<<<ARTICLE_END>>>
```

---

## 5. وظائف الأداة (Tool Capabilities)

### 5.1 ما تفعله الأداة — UPDATED v1.1

**التحليل (Parsing):**
- قراءة النص الملصق وتحديد كل قسم بناءً على العلامات
- التحقق من وجود كل الأقسام المطلوبة
- التحقق من أن المحتوى العربي فعلاً عربي والإنجليزي فعلاً إنجليزي
- **[v1.1]** التحقق من وجود الحقول الإضافية عند TYPE: CASE
- عرض رسائل خطأ واضحة بالعربي لو في مشكلة

**التصنيف (Classification):**
- عرض التصنيفات المقترحة من NotebookLM كأزرار ملونة
- السماح للمستخدم بتعديل التصنيفات (إضافة أو حذف)
- فرض القيود: category واحد فقط، brands حتى 3، symptoms حتى 5

**توليد الـ Slug:**
- تحويل العنوان الإنجليزي تلقائياً إلى slug
- إضافة بادئة القسم (مثلاً hdd-wd-clicking-sound)
- **[v1.1]** إضافة بادئة `case-` لقصص النجاح
- فحص التكرار مقابل سجل محلي في المتصفح (localStorage)
- السماح للمستخدم بتعديل الـ slug يدوياً

**معالجة الصور:**
- عرض كل اقتراح صورة مع وصفه والبرومبت بتاعه
- السماح برفع صورة من الجهاز لكل موضع
- أو لصق صورة من الحافظة (Clipboard)
- أو تجاوز (المقال ينشر بدون صورة في هذا الموضع)
- إعادة تسمية الصورة المرفوعة تلقائياً لتطابق الـ slug
- الصورة الأولى تكون الـ heroImage تلقائياً

**حقن المكونات (Component Injection) — UPDATED v1.1:**
- تحويل `:::warning` إلى `<WarningBox>`
- حقن `<PanicButton />` بعد أول H2 إذا كان difficulty هو critical (مقالات Posts فقط)
- حقن `<GentleNote />` قبل نهاية المقال (للمقالات والخدمات وقصص النجاح)
- حقن `<ArticleDisclaimer />` في نهاية المقال (Posts فقط)
- تحويل `<<<REEL_URL: url>>>` إلى `<ReelEmbed url="..." platform="..." />`
- توليد سطور import المكونات تلقائياً في أعلى الملف

**توليد Frontmatter:**
- بناء كل حقول الـ frontmatter تلقائياً
- التاريخ = تاريخ اليوم
- translationID = الـ slug
- heroImage = مسار الصورة الأولى
- **[v1.1]** حقول Cases الإضافية عند TYPE: CASE

**التحقق من المحتوى — NEW v1.1:**
- فحص الممنوعات اللغوية (Section 10.4) — تحذير
- فحص ذكر الأماكن (Section 10.5) — تحذير

**التصدير:**
- توليد ملف ZIP يحتوي على كل شيء في مساراته الصحيحة
- أو النشر المباشر عبر GitHub API (مرحلة لاحقة)

### 5.2 ما لا تفعله الأداة

- لا تكتب المحتوى (هذا دور NotebookLM)
- لا تصمم أو تولّد الصور (هذا دور المستخدم + أدوات AI خارجية)
- لا تعدّل الصفحات الثابتة (الرئيسية، من نحن، اتصل بنا)
- لا تعدّل مقالات سابقة (في المرحلة الأولى)
- لا تعمل كـ CMS كامل
- **[v1.1]** لا تحقن FloatingWhatsApp (هذا مكون layout عام يتولاه الموقع مباشرة)
- **[v1.1]** لا تعدّل location.ts (هذا ملف تكوين يُعدَّل يدوياً)

---

## 6. المكونات المدعومة (Supported Components) — UPDATED v1.1

### 6.1 القائمة الكاملة

هذه المكونات مأخوذة من UX v3 (ملف 04) و Content Plan v1.3 (ملف 07) و Homepage Blueprint v1.0 (ملف 09). الأداة تحقنها تلقائياً حسب القواعد المحددة.

**PanicButton** (In-Article Emergency Banner — NOT the floating button) — UPDATED v1.1
- المصدر: UX v3, TAD v4.2
- الوظيفة: شريط طوارئ بعرض الصفحة داخل محتوى المقال. يعرض رسالة قوية تطلب من القارئ التوقف والتواصل مع Datacodex. هذا المكون **مختلف تماماً** عن أيقونة واتساب العائمة الصغيرة (FloatingWhatsApp) التي تظهر في كل الصفحات.
- متى يُحقن: تلقائياً بعد أول H2 إذا كان difficulty هو critical
- ينطبق على: Posts فقط (ليس Services، ليس Cases)
- الشكل: شريط أخضر بعرض الصفحة مع زر واتساب كبير
- النص: حسب السياق (مثلاً "هل تسمع صوت طقطقة الآن؟ افصل الجهاز فوراً")
- الملف: `@components/content/PanicButton.astro`
- الأداة تحقن `<PanicButton />` — المكون يقرأ category من frontmatter ليختار النص المناسب.

> **ملاحظة مهمة:** أيقونة واتساب العائمة الصغيرة التي تظهر في كل صفحات الموقع هي مكون layout اسمه FloatingWhatsApp. الأداة **لا تحقنه**. هو جزء من قالب الموقع (AstroWind layout) ويُدار من BaseLayout.astro.

**WarningBox**
- المصدر: UX v3
- الوظيفة: صندوق تحذير أحمر من المحاولات اليدوية
- متى يُحقن: عندما يكتب NotebookLM `:::warning` في المحتوى
- يُحقن أيضاً تلقائياً في بداية المقال إذا كان difficulty هو critical ولم يكتب NotebookLM أي warning
- ينطبق على: Posts فقط
- الملف: `@components/content/WarningBox.astro`
- Props: `title` (اختياري — نص عنوان الصندوق)

**GentleNote** (NEW v1.1 — replaces CallToAction in articles)
- المصدر: Homepage Blueprint v1.0 (ملف 09)، Content Plan v1.3
- الوظيفة: ملاحظة هادية وسياقية تقترح التواصل مع Datacodex. **ليست** دعوة بيعية. هي رسالة "لو محتاج مساعدة، إحنا موجودين" بنبرة معرفية.
- متى يُحقن: تلقائياً بعد آخر H2 أو قبل نهاية المقال
- ينطبق على: Posts، Services، وCases
- **لا** ينطبق على: الصفحة الرئيسية، صفحة About
- السلوك:
  - النص يتغير تلقائياً حسب category و symptoms من frontmatter المقال
  - بيانات المكان (المدينة، العنوان) تُقرأ من ملف location.ts
  - عند تغيير المكان، كل نسخ GentleNote تتحدث تلقائياً
- أمثلة النصوص حسب التصنيف:
  - **HDD + clicking:** "لو بتسمع صوت طقطقة دلوقتي، ما تحاولش تشغل الهارد تاني. Datacodex متخصص في الحالات دي. تواصل عبر واتساب أو زورنا في [location]."
  - **HDD + not-detected:** "لو الكمبيوتر مش شايف الهارد، ما تحاولش تفكه بنفسك. Datacodex يقدر يفحصه بأجهزة متخصصة. تواصل عبر واتساب أو زورنا في [location]."
  - **SSD:** "مشاكل الـ SSD غالباً بتحتاج أدوات متخصصة. Datacodex عنده الأجهزة المناسبة. تواصل عبر واتساب أو زورنا في [location]."
  - **RAID:** "لو السيرفر وقع، ما تحاولش تعمل rebuild بنفسك. كل خطوة غلط بتقلل فرص الاستعادة. تواصل عبر واتساب أو زورنا في [location]."
  - **Flash:** "لو الفلاشة أو الكارت مش شغالين، ما تعملش فورمات. فيه فرصة للاستعادة. تواصل عبر واتساب أو زورنا في [location]."
  - **General:** "لو محتاج مساعدة متخصصة في استعادة البيانات، Datacodex موجود. تواصل عبر واتساب أو زورنا في [location]."
  - كل النصوص تنتهي بـ: "تواصل عبر واتساب أو زورنا في [location]."
- `[location]` يُقرأ من ملف location.ts — يتغير عندما ينتقل Datacodex لمكان جديد
- الشكل: خلفية رمادية فاتحة، نص صغير، **ليس** بارزاً، **ليس** أحمر أو أخضر
- الأيقونة: 💡 أو ℹ️
- الملف: `@components/content/GentleNote.astro`
- الأداة تحقن `<GentleNote />` — بدون props. المكون يقرأ category و symptoms من frontmatter المقال و location من location.ts وقت البناء.

**ArticleDisclaimer**
- المصدر: Content Plan v1.3
- الوظيفة: إخلاء مسؤولية قانونية
- متى يُحقن: تلقائياً في نهاية كل مقال (Posts فقط، ليس Services أو Cases)
- الملف: `@components/content/ArticleDisclaimer.astro`
- لا يأخذ props

**VideoEmbed**
- المصدر: TAD v4
- الوظيفة: تضمين فيديو يوتيوب
- متى يُحقن: يدوياً فقط — لا يتم حقنه تلقائياً
- العلامة: `<<<VIDEO_ID: id>>>` في المحتوى
- الملف: `@components/content/VideoEmbed.astro`
- Props: `id` (معرف فيديو يوتيوب)

**ReelEmbed** — NEW v1.1
- المصدر: Homepage Blueprint v1.0 (ملف 09)
- الوظيفة: تضمين ريل (فيديو قصير) من TikTok أو Instagram داخل المقال
- العلامة: `<<<REEL_URL: https://tiktok.com/...>>>` في المحتوى
- متى يُحقن: عند وجود العلامة في المحتوى
- ينطبق على: Posts و Cases
- الملف: `@components/content/ReelEmbed.astro`
- Props: `url` (رابط الريل)، `platform` (tiktok أو instagram — يُحدد تلقائياً)
- الأداة تحوّل العلامة إلى `<ReelEmbed url="..." platform="tiktok" />`
- المنصة تُحدد تلقائياً من الرابط: `tiktok.com` → tiktok، `instagram.com` → instagram

### 6.2 قواعد الحقن — UPDATED v1.1

```
المقالات (Posts):
┌──────────────────────────────┐
│ ---frontmatter---            │
│ import statements            │
│                              │
│ [WarningBox] ← إذا critical │
│                              │
│ ## أول عنوان                 │
│                              │
│ [PanicButton] ← إذا critical│
│                              │
│ ... محتوى المقال ...         │
│ [:::warning → WarningBox]    │
│ [<<<IMAGE>>> → صورة]         │
│ [<<<REEL_URL>>> → ReelEmbed] │
│                              │
│ [GentleNote]                 │
│ [ArticleDisclaimer]          │
└──────────────────────────────┘

الخدمات (Services):
┌──────────────────────────────┐
│ ---frontmatter---            │
│ import statements            │
│                              │
│ ... محتوى الخدمة ...        │
│                              │
│ [GentleNote]                 │
│ (لا يوجد Disclaimer)        │
└──────────────────────────────┘

قصص النجاح (Cases): — NEW v1.1
┌──────────────────────────────┐
│ ---frontmatter---            │
│ import statements            │
│                              │
│ ... محتوى القصة ...         │
│ [<<<IMAGE>>> → صورة]         │
│ [<<<REEL_URL>>> → ReelEmbed] │
│                              │
│ [GentleNote]                 │
│ (لا يوجد Disclaimer)        │
└──────────────────────────────┘
```

**ملخص المكونات حسب نوع المحتوى:**

| المكون | Posts | Services | Cases |
|--------|-------|----------|-------|
| PanicButton (إذا critical) | ✅ | ❌ | ❌ |
| WarningBox (إذا critical) | ✅ | ❌ | ❌ |
| GentleNote | ✅ | ✅ | ✅ |
| ArticleDisclaimer | ✅ | ❌ | ❌ |
| VideoEmbed (إذا وُجدت علامة) | ✅ | ✅ | ✅ |
| ReelEmbed (إذا وُجدت علامة) | ✅ | ❌ | ✅ |

---

## 7. معالجة الصور (Image Handling) — UPDATED v1.1

### 7.1 المسارات

وفقاً لقرار نقل الصور إلى src/assets (مسجل في TAD v4.2):

```
src/
  assets/
    images/
      posts/
        {slug}.webp          ← hero image
        {slug}-2.webp         ← صورة داخلية ثانية
        {slug}-3.webp         ← صورة داخلية ثالثة
      services/
        {slug}.webp
      cases/                  ← NEW v1.1
        {slug}.webp           ← hero image
        {slug}-2.webp         ← صورة إضافية (لو وجدت)
```

### 7.2 قواعد التسمية

- اسم الصورة = الـ slug
- الصورة الأولى (hero): `{slug}.webp`
- الصور الإضافية: `{slug}-2.webp`, `{slug}-3.webp`
- الأسماء دائماً بالإنجليزي (متطلب SEO من ملف 06)
- الأسماء وصفية وليست عشوائية (مثلاً `wd-clicking-drive-recovery.webp` وليس `img001.webp`)

### 7.3 المواصفات التقنية

- الأبعاد المفضلة للـ hero: 1200x630px (نسبة 1.91:1 — مثالية لمشاركات السوشيال)
- الصيغ المقبولة: JPG, PNG, WebP
- Astro يقوم بالتحسين وقت البناء (build-time optimization) — لذلك لا تحتاج الأداة لضغط الصور
- الحد الأقصى لحجم الصورة المرفوعة: 5MB (تحذير فقط، ليس منع)

### 7.4 سلوك الأداة مع الصور

1. الأداة تقرأ كل بلوك `<<<IMAGE>>>` من النص
2. تعرض مربع لكل صورة يحتوي على:
   - وصف الصورة (من NotebookLM)
   - نوع الصورة المقترح
   - زر "رفع من الجهاز"
   - زر "لصق من الحافظة"
   - زر "تجاوز — بدون صورة"
   - البرومبت الإنجليزي (قابل للنسخ لاستخدامه في أداة AI خارجية)
3. عند الرفع، الأداة تعيد تسمية الملف تلقائياً
4. في الملف MDX الناتج، الصورة تُدرج بكود Astro:
   ```mdx
   ![النص البديل](../../assets/images/posts/{slug}.webp)
   ```

### 7.5 Image Policy — NEW v1.1

All website images are AI-generated. No personal photos.
No photos that identify a specific physical location.

The tool stores the AI prompt used to generate each image alongside
the image metadata. This allows regenerating or modifying images later.

Image metadata stored per image:

| الحقل | النوع | الوصف |
|-------|-------|-------|
| filename | string | اسم الملف — يُولّد تلقائياً من الـ slug |
| alt_ar | string | نص بديل بالعربي (للـ SEO وإمكانية الوصول) |
| alt_en | string | نص بديل بالإنجليزي |
| ai_prompt | string | البرومبت المستخدم لتوليد الصورة بالذكاء الاصطناعي |
| type | enum | hero / inline / card / reel-thumbnail |

هذه البيانات تُستخرج من بلوك `<<<IMAGE>>>` وتُحفظ في localStorage للرجوع إليها لاحقاً.

---

## 8. سير العمل الكامل (Complete Workflow)

### 8.1 المرحلة الأولى — MVP — UPDATED v1.1

```
صاحب الموقع                        النظام
─────────────                      ────────
يفتح NotebookLM                      │
يطلب مقال/خدمة/قصة نجاح              │
      │                               │
      ▼                               │
ينسخ الناتج كاملاً                   │
      │                               │
      ▼                               │
يفتح الأداة في المتصفح               │
(صفحة في الموقع أو ملف محلي)          │
      │                               │
      ▼                               │
يلصق النص ← يضغط "تحليل"            │
      │                               ▼
      │                        الأداة تحلل النص
      │                        تتحقق من الصيغة
      │                        تستخرج كل قسم
      │                        [v1.1] تفحص الممنوعات اللغوية
      ▼                               │
يراجع التصنيفات                      │
(أزرار ملونة — يعدل لو محتاج)        │
      │                               │
      ▼                               │
يراجع الـ slug                        │
(يعدل لو محتاج)                      │
      │                               │
      ▼                               │
يرفع الصور أو يتجاوز                 │
      │                               │
      ▼                               │
يضغط "تحميل ZIP"                     │
      │                               ▼
      │                     الأداة تولد الملفات:
      │                     ├── ar/{slug}.mdx (+ GentleNote محقون)
      │                     ├── en/{slug}.mdx (+ GentleNote محقون)
      │                     └── images/{slug}.webp
      │                     تنزل ملف ZIP
      ▼                               │
يفك الـ ZIP في مجلد المشروع           │
يفتح GitHub Desktop                   │
يعمل commit + push                    │
      │                               ▼
      │                     Cloudflare يرصد التغيير
      │                     يبني الموقع (< 1 دقيقة)
      │                               │
      ▼                               ▼
يتأكد إن المحتوى ظاهر         المحتوى حي على الموقع
```

### 8.2 المرحلة التالية — نشر مباشر

نفس سير العمل بس بدل "تحميل ZIP" يكون في زرار "نشر مباشرة". الأداة تتصل بـ Cloudflare Worker. الـ Worker يرفع الملفات على GitHub. التوكن مخزن في متغيرات Cloudflare (Secrets). المستخدم لا يتعامل مع GitHub أبداً.

---

## 9. واجهة الأداة (Tool UI) — UPDATED v1.1

### 9.1 الفلسفة

الأداة تعمل كـ Wizard من 4 خطوات. كل خطوة بسيطة ومركزة. المستخدم لا يرى كل شيء مرة واحدة. الانتقال بين الخطوات بأزرار "التالي" و"السابق".

### 9.2 الخطوات

**الخطوة 1 — اللصق والتحليل**
- مربع نص كبير (textarea)
- زرار "تحليل المحتوى"
- عرض نتيجة التحليل (نجاح / تحذيرات / أخطاء)
- **[v1.1]** عرض تحذيرات الممنوعات اللغوية (إن وُجدت)
- **[v1.1]** عرض تحذيرات ذكر الأماكن (إن وُجدت)
- نسبة ثقة: الأداة تعطي نسبة مئوية لثقتها في صحة الإدخال

**الخطوة 2 — التصنيف والمراجعة**
- عرض العناوين والأوصاف (قابلة للتعديل)
- أزرار تصنيف ملونة (Chip Selector):
  - بنفسجي: القسم الرئيسي (اختيار واحد)
  - أخضر: البراندات (اختيار متعدد حتى 3)
  - برتقالي: الأعراض (اختيار متعدد حتى 5)
  - أحمر/أصفر/أخضر: مستوى الصعوبة (اختيار واحد)
- حقل الـ slug (قابل للتعديل + فحص تكرار)
- اختيار نوع المحتوى (مقال / خدمة / قصة نجاح) — إذا لم يحدده NotebookLM
- **[v1.1]** حقول Cases الإضافية (تظهر عند اختيار "قصة نجاح"):
  - device_type (dropdown)
  - recovery_percentage (number input)
  - badge (success / challenge)
  - problem, solution, result (text inputs)

**الخطوة 3 — الصور**
- مربع لكل صورة مقترحة
- وصف الصورة + البرومبت
- أزرار: رفع / لصق / تجاوز
- معاينة الصورة المرفوعة

**الخطوة 4 — المراجعة النهائية والتصدير**
- معاينة الملف العربي (frontmatter + بداية المحتوى)
- معاينة الملف الإنجليزي
- **[v1.1]** عرض المكونات المحقونة (GentleNote, PanicButton, etc.)
- قائمة الملفات اللي هتكون في الـ ZIP
- زرار "تحميل ZIP"
- (مستقبلاً: زرار "نشر مباشرة")

---

## 10. التحقق والحماية من الأخطاء (Validation) — UPDATED v1.1

### 10.1 أخطاء تمنع التوليد (Blocking Errors)

- النص لا يحتوي على علامة `<<<ARTICLE_START>>>`
- العنوان العربي أو الإنجليزي مفقود
- المحتوى العربي أو الإنجليزي مفقود
- القسم الرئيسي (category) غير محدد
- مستوى الصعوبة (difficulty) غير محدد
- الـ slug فارغ أو يحتوي على أحرف غير مسموحة
- **[v1.1]** TYPE هو CASE ولكن الحقول الإلزامية مفقودة (device_type, recovery_percentage, badge, problem, solution, result)

### 10.2 تحذيرات لا تمنع التوليد (Warnings)

- الوصف أطول من 160 حرف (يؤثر على SEO)
- الوصف أقصر من 50 حرف
- لا توجد صورة hero
- الـ slug مكرر في السجل المحلي
- المحتوى العربي نسبة العربي فيه أقل من 30%
- لا يوجد brands محدد
- لا يوجد symptoms محدد

### 10.3 إصلاحات تلقائية (Auto-fixes)

- حذف أي أرقام مراجع بين أقواس مربعة [1] [2] (بقايا من NotebookLM)
- تنظيف المسافات الزائدة
- توحيد علامات العناوين (## بدل ##)
- إضافة سطر فارغ قبل وبعد العناوين (مطلوب لـ Markdown)

### 10.4 الممنوعات اللغوية (Content Policy Validation) — NEW v1.1

The tool should **WARN** (not block) if the content contains any of these
selling-language phrases:

**عبارات عربية ممنوعة:**

| العبارة | السبب |
|---------|-------|
| "فحص مجاني" | لغة عروض — ليست لغة موسوعة |
| "احجز ميعاد" | أمر بيعي مباشر |
| "اتصل الآن" | إلحاح زمني |
| "عروضنا" | لغة تجارية بحتة |
| "أسعارنا" | الموسوعة لا تتحدث عن أسعار |
| "أفضل محل" | مبالغة تسويقية |
| "رقم 1 في جدة" | ادعاء غير قابل للتحقق |
| "تفضل بزيارة" | دعوة بيعية |
| "ابدأ الفحص" | أمر بيعي مباشر |

These are **warnings, not blockers**. The user can choose to proceed.
Reason: NotebookLM is trained to avoid these (see Content Plan v1.3 persona), but mistakes happen.

**عرض التحذير:**
- الأداة تعرض كل عبارة ممنوعة وُجدت مع رقم السطر
- لون التحذير: برتقالي (ليس أحمر)
- زر "تجاهل والمتابعة" متاح دائماً

### 10.5 Location Mentions in Content (Warning) — NEW v1.1

The tool should **WARN** if the article body content (not frontmatter) contains
specific location names:

| الكلمة/العبارة | السبب |
|----------------|-------|
| "جدة" | يجب أن تأتي من location.ts عبر GentleNote فقط |
| "حي الشرفية" | اسم حي — يتغير عند الانتقال |
| "مركز الفارس" | اسم مركز — يتغير عند الانتقال |
| "شارع خالد بن الوليد" | اسم شارع — يتغير عند الانتقال |

**السبب:** بيانات المكان يجب أن تأتي من ملف location.ts عبر مكون GentleNote وليست مكتوبة يدوياً في نص المقال. هذا يضمن أنه عندما ينتقل Datacodex لمكان جديد، كل المقالات تتحدث تلقائياً بدون تعديل.

**الاستثناء:** صفحة `/jeddah/` وصفحة `/contact/` — هاتان مسموح فيهما ذكر المكان. لكن هاتان ليستا من إنتاج الأداة (صفحات ثابتة).

---

## 11. إدارة الروابط (Slug Management) — UPDATED v1.1

### 11.1 توليد الـ Slug

```
Input:  "Data Recovery from Western Digital HDD with Clicking Sound"
         + category: "hdd"
         + type: "post"

Output: "hdd-data-recovery-from-western-digital-with-clicking-sound"
```

```
Input:  "WD Hard Drive — 1.5 Meter Drop Recovery"
         + category: "hdd"
         + type: "case"

Output: "case-hdd-wd-hard-drive-drop-recovery"
```

القواعد:
- **[v1.1]** بادئة `case-` لقصص النجاح + بادئة القسم + العنوان الإنجليزي
- بادئة القسم + العنوان الإنجليزي (للمقالات والخدمات)
- أحرف صغيرة فقط (lowercase)
- مسافات تتحول لشرطات
- حذف كل الأحرف الخاصة
- حد أقصى 80 حرف
- لا يبدأ أو ينتهي بشرطة

### 11.2 منع التكرار

**الطبقة الأولى:** البادئة + الاسم الوصفي يقللان احتمال التكرار طبيعياً.

**الطبقة الثانية:** سجل محلي في localStorage يحتفظ بكل الـ slugs اللي اتولدت. الأداة تفحص مقابله وتحذر لو لقيت تكرار.

**الطبقة الثالثة:** Astro نفسه يرفض بناء الموقع لو لقى ملفين بنفس الاسم في نفس المجلد. ده شبكة أمان أخيرة.

**الطبقة الرابعة (مستقبلي):** الأداة تجلب قائمة الملفات من GitHub API وتفحص مقابلها.

### 11.3 Location Config Reference — NEW v1.1

The tool does **NOT** manage location.ts directly. However, it should be
aware that GentleNote reads from this config file. The tool's role is
to inject `<GentleNote />` — the component handles the rest at build time.

| المعلومة | القيمة |
|----------|--------|
| ملف التكوين | `src/config/location.ts` |
| المحتوى | المدينة، العنوان، رقم واتساب، ساعات العمل، رابط الخريطة، نصوص GentleNote المخصصة |
| من يقرأه | GentleNote.astro, Footer.astro, FloatingWhatsApp.astro, صفحة Contact, صفحة Jeddah, SchemaLocalBusiness.astro |

**عند انتقال Datacodex لمكان جديد:**
- يُعدَّل ملف location.ts فقط (يدوياً)
- كل نسخ GentleNote في كل المقالات تتحدث تلقائياً
- صفحة Contact تتحدث تلقائياً
- الـ Footer يتحدث تلقائياً
- Schema LocalBusiness يتحدث تلقائياً

**الأداة لا تحتاج تعرف محتوى location.ts** — فقط تحقن `<GentleNote />` والمكون يتولى الباقي.

---

## 12. هيكل الـ ZIP الناتج (Output Structure) — UPDATED v1.1

### 12.1 لمقال (Post)

```
drhd-post-{slug}-{timestamp}.zip
│
├── src/
│   └── content/
│       └── posts/
│           ├── ar/
│           │   └── {slug}.mdx
│           └── en/
│               └── {slug}.mdx
│
└── src/
    └── assets/
        └── images/
            └── posts/
                ├── {slug}.webp        ← hero image
                ├── {slug}-2.webp      ← صورة إضافية (لو وجدت)
                └── {slug}-3.webp      ← صورة إضافية (لو وجدت)
```

### 12.2 لخدمة (Service)

```
drhd-service-{slug}-{timestamp}.zip
│
├── src/
│   └── content/
│       └── services/
│           ├── ar/
│           │   └── {slug}.mdx
│           └── en/
│               └── {slug}.mdx
│
└── src/
    └── assets/
        └── images/
            └── services/
                └── {slug}.webp
```

### 12.3 لقصة نجاح (Case) — NEW v1.1

```
drhd-case-{slug}-{timestamp}.zip
│
├── src/
│   └── content/
│       └── cases/
│           ├── ar/
│           │   └── {slug}.mdx
│           └── en/
│               └── {slug}.mdx
│
└── src/
    └── assets/
        └── images/
            └── cases/
                ├── {slug}.webp        ← hero image
                └── {slug}-2.webp      ← صورة إضافية (لو وجدت)
```

---

## 13. خطة التطوير على مراحل (Development Phases) — UPDATED v1.1

### المرحلة 0 — بناء الموقع (قبل الأداة)

- بناء هيكل الموقع بالكامل
- تصميم كل الصفحات والمكونات
- كتابة 3-5 مقالات يدوياً
- نشر الموقع على Cloudflare Pages
- هذه المقالات تكون "المقياس" اللي نبني عليه الأداة

### المرحلة 1 — الأداة الأساسية — UPDATED v1.1

- ملف HTML واحد مستقل
- دعم ثلاثة أنواع محتوى: **Posts, Services, Cases**
- كل نوع له نموذج خاص بحقوله (Cases تعرض حقول إضافية)
- لصق + تحليل + تصنيف + صور + تصدير ZIP
- حقن المكونات: GentleNote, PanicButton, WarningBox, ArticleDisclaimer, ReelEmbed
- فحص الممنوعات اللغوية وذكر الأماكن (تحذيرات)
- بدون اتصال بأي خدمة خارجية
- يعمل Offline
- النشر يدوي عبر GitHub Desktop

### المرحلة 2 — الأداة كصفحة في الموقع

- نقل الأداة من ملف محلي إلى صفحة في الموقع
- رابط سري (مثلاً /dx7workshop)
- نفس الوظائف + تجربة أفضل (دائماً آخر نسخة)

### المرحلة 3 — النشر المباشر

- إضافة Cloudflare Worker كوسيط
- التوكن في متغيرات Cloudflare (Secrets)
- زرار "نشر مباشرة" في الأداة
- لا حاجة لـ GitHub Desktop
- إمكانية النشر من الموبايل

### المرحلة 4 — قراءة المحتوى الموجود

- الأداة تجلب قائمة المقالات من GitHub API
- فحص تكرار الـ slugs مقابل القائمة الحية
- عرض إحصائيات (عدد المقالات لكل قسم)
- إمكانية تعديل مقال قديم (فتح + تعديل + إعادة نشر)

### المرحلة 5 — أنواع محتوى وأدوات مستقبلية

> **[v1.1] تم نقل Cases من هذه المرحلة إلى المرحلة 1.**

- دعم أنواع محتوى جديدة حسب الحاجة (مثلاً: FAQs مستقلة، Glossary)
- أدوات مساعدة إضافية (مثلاً: مولّد عناوين SEO، محلل منافسين)
- تكامل مع أدوات AI لتوليد الصور مباشرة من الأداة

---

## 14. المرجع المشترك (Schema Contract)

### 14.1 المبدأ

يجب أن يكون هناك "مصدر واحد للحقيقة" (Single Source of Truth) لقوائم التصنيفات وقواعد الحقول. هذا المرجع يُستخدم في مكانين:
1. داخل الأداة (JavaScript) — للتحقق من الإدخال
2. داخل Astro (content.config.ts) — للتحقق وقت البناء

### 14.2 التنفيذ

يوجد ملف واحد اسمه `schema-contract` يحتوي على كل القوائم والقيود. هذا الملف هو المرجع. لو تم تعديل قائمة البراندات (مثلاً إضافة علامة تجارية جديدة)، يجب تعديل هذا الملف ثم تحديث الأداة و Astro config معاً.

**[v1.1]** يجب أن يشمل المرجع أيضاً:
- قائمة device_type المسموحة (hdd, ssd, raid, flash)
- قائمة badge المسموحة (success, challenge)
- قائمة الممنوعات اللغوية (Section 10.4)
- قائمة أسماء الأماكن المحظورة (Section 10.5)

---

## 15. ملاحظات مهمة

### 15.1 ما يجب أن يعرفه المطور

- الـ slug يجب أن يكون إنجليزي دائماً حتى للملفات العربية (قرار من SEO v4)
- الـ translationID يجب أن يتطابق بالضبط في الملفين العربي والإنجليزي
- مسار الصورة في الـ frontmatter يبدأ بـ `../../assets/images/` وليس `/images/`
- المقال الإنجليزي ليس ترجمة حرفية — هو مقال مستقل بأسلوب إنجليزي
- الصفحات الثابتة (الرئيسية، من نحن، اتصل بنا) ليست من شغل الأداة
- **[v1.1]** GentleNote يحل محل CallToAction — الأداة تحقن `<GentleNote />` وليس `<CallToAction />`
- **[v1.1]** PanicButton هو شريط طوارئ داخل المقال — مختلف عن FloatingWhatsApp (الزر العائم)
- **[v1.1]** FloatingWhatsApp هو مكون layout — الأداة لا تتعامل معه
- **[v1.1]** location.ts هو مصدر بيانات المكان — الأداة لا تعدّله لكن تعرف أن GentleNote يقرأ منه
- **[v1.1]** كل الصور AI-generated — لا صور شخصية ولا صور تحدد مكان فعلي

### 15.2 علاقة هذا الملف بالملفات الأخرى — UPDATED v1.1

| الملف | العلاقة |
|-------|---------|
| 01_Vision | لا تأثير |
| 02_Risk | يُكمله — الأداة هي الحل للمخاطر المسجلة |
| 03_PRD | لا تأثير |
| 04_UX | يعتمد عليه — المكونات مأخوذة منه |
| 05_TAD v4.2 | يُعدّله — إضافة حقول frontmatter + حذف panicLevel. **[v1.1]** يتوافق مع GentleNote وFloatingWhatsApp وCases |
| 06_SEO | يلتزم به — لا تعديل |
| 07_Content_Plan v1.3 | يُعدّله — استبدال Keystatic بالأداة. **[v1.1]** يتوافق مع GentleNote والممنوعات اللغوية |
| 09_Homepage_Blueprint | **[v1.1 NEW]** يعتمد عليه — GentleNote وReelEmbed مأخوذان منه |

---

**نهاية الملف — 08_Content_Tool_Spec v1.1**

---

## ملاحظات حول التعارضات — Conflict Notes

> **تعارض مُكتشف ومُعالج:**
>
> في تعليمات التغيير 1، وصف مكون GentleNote يقول:
> "Applies to: Posts and Cases (Success Stories)" و "NOT applied to: Services"
>
> لكن مخطط الحقن في نفس التعليمات (القسم 6.2) يعرض GentleNote داخل مخطط Services:
> ```
> الخدمات (Services):
> │ [GentleNote]                 │
> ```
>
> كذلك، ملفات المشروع الأخرى (TAD v4.2 §4.4, Content Plan v1.3 §4.E) تؤكد أن
> GentleNote يُحقن في "all posts and services".
>
> **القرار:** تم اتباع مخططات الحقن (الأكثر تفصيلاً) والتوافق مع TAD v4.2
> وContent Plan v1.3. GentleNote يُحقن في **Posts وServices وCases** جميعاً.
> تم تعديل وصف المكون في القسم 6.1 ليتطابق مع المخططات.